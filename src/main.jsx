import React from "react";
import ReactDOM from "react-dom/client";
import CaseAstaApp from "../case-asta.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CaseAstaApp />
  </React.StrictMode>
);
